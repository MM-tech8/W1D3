const printWord = require ("./testing.js");

const printWord2 = printWord ();

// Its taking all the functions in the file and executing them instead of just the printWord function

const celsiusToFahrenheit = require ("./testing.js");

const cTF = celsiusToFahrenheit (2);
// Function has to be called upon with a parameter here for it to execute properly
